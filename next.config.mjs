/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '', // Set this if your app is not hosted at the root domain
    assetPrefix: '', // Set this if your assets are not hosted at the root domain
};

export default nextConfig;
